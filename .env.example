# 面板后端（aidcp-cloud panel API 层）地址，本地开发时 vite 反代 /api 与 /ws 到此。
# 生产由 Nginx 反代，无需设置。
VITE_API_TARGET=http://127.0.0.1:8090

# 漂移测试（aidcp-enums.test.ts）对 live /api/version 断言时使用；不设则该用例跳过。
# AIDCP_PANEL_URL=http://127.0.0.1:8090
