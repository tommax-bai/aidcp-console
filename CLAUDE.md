# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> 本仓是 **aidcp\*** 家族的一员（管理后台前端）。家族级契约、架构与完整工作约定以中控仓 `../aidcp/CLAUDE.md` 为权威；本文件只固化「跨仓通用、需随 git 换机器仍生效」的沟通偏好，确保在本仓直接工作时也加载。

## 沟通方式（默认模式，用户偏好）

- **问题 / 根因 / 方案的说明方式**：讲逻辑、讲因果，**不用比喻**；**不点代码内部标识符**（变量 / 类 / 函数 / 消息类型名），改用**功能性正文**描述组件与机制（如「执行端 / 决策端 / 监测体」「发命令给执行端的统一出口」「会话凭证失效后的统一兜底」）。
- 分点、句子短，让非工程视角也能跟上；确需落到代码时再补具体 `文件:行`，且放在解释之后。诊断阶段重逻辑，实现阶段才落到具体文件。
- 正文默认中文；代码 / 注释 / commit / PR / 命令 / 文件名保持英文。

## 表格 / 视觉设计标准（重要，用户 2026-07-08 定）

- **管理后台表格尽可能不换行（重要设计标准）**：单元格与列头都以「单行不换行」为目标。优先手段是**收短文案**（去冗余后缀 / 前缀），而**不是缩小间距或压字号**去硬塞。
  - 列头即语境：值徽标不再重复列名。已落地示例——列头「风控」「档位」（原「风控状态」「配额档位」），徽标去掉「状态：」「档位：」前缀；「信号数」→「信号」；人设列去「人设」后缀（需设置 / 已绑 / 默认）。
  - 撞名值（如风控态与档位都有「正常」）的消歧改由**色相 + 形态 + 所在列表头**承担，不靠文字前缀：风控态用暖色实底 `Tag`，档位用冷色描边 `Tag`（见 `RiskStatusBadge` / `QuotaTierBadge`）。
  - 新增 / 改列时：先想能否让文案更短、让列头承担语境；确需更多水平空间给列显式 `width` 或让内容 `white-space: nowrap`，而不是让它换行。
- **可点元素要一眼可点（近黑品牌下的 affordance）**：主色近黑（`colorLink=#030213`），链接默认与正文同色又无下划线，会渲染成「看不出可点」的纯文本。统一走 `src/styles/app.css` 两个类，别再裸写 `<Link>`/`<a>`：
  - `.table-link-chip` —— 站内导航「动作簇」（如账号页「站内视图」）：小号胶囊按钮，静息即带描边 + 浅底 + 圆角。
  - `.ext-link` —— 行内名字/标题外链（`ProfileLink`、互动目标标题）：下划线 + 悬停高亮 + 尾随 ↗。
  - **名字/标题类外链不套 `<Tag>` 胶囊**：胶囊(小按钮语义) + 下划线(链接语义)两种可点样式叠加会显重复/杂乱，指向**外部用户主页**的名字（联系人昵称、互动目标作者）只用 `.ext-link` 下划线（`站内视图`那类**动作簇**才用 `.table-link-chip` 胶囊）。
  - **「归属标注」类不做可点**：自己账号的名/ID（如内容页、联系人页的「账号」列）是分组/归属信息、非导航目标——用**纯 `<Tag>` 标签**展示、不套 `ProfileLink`、不拦截整行点击。可点性只留给真能跳去某处的元素。
  - 不引入蓝 / 青强调色（保持近黑品牌）；纯文本回落（无有效链接）绝不加可点样式，以免误导。

## 提交 / 推送 / 部署

- **提交 / 推送 / 部署默认直接做、不用逐次问**（用户长期授权，2026-06-27）。推默认分支 `master`；commit message 末尾带 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。
- 部署形态与安全序列（先备份 → 同步 → 验证 → 失败回滚）、isales 红线、生产机访问细节，以中控仓 `../aidcp/CLAUDE.md` §5/§6 为权威。
