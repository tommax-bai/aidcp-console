import { Empty } from 'antd';

/** 账号列表 + 详情（task 6.5 / task 5 接入 /api/accounts）。两个独立徽标 status/tier 在此渲染。 */
export function AccountsPage() {
  return <Empty description="Accounts — 待 task 5 只读接口 /api/accounts(+/:id) 接入" />;
}
