import { Empty } from 'antd';

/** 实时日志流（panel-WS 单一全局流 + 客户端过滤）+ 操作时间线（task 6.4 / task 5.2 接入）。 */
export function MonitorPage() {
  return <Empty description="Monitor — 待 task 5.2 面板 WS（单一全局流）接入" />;
}
